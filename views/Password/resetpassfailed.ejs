<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reset password failed</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            text-align: center;
        }

        .container {
            background-color: #fff;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
            padding: 20px;
            margin: 20px auto;
            max-width: 400px;
        }

        h1 {
            color: #FF0000; /* Red color for error message */
        }

        p {
            color: #666;
        }

        .button-container {
            margin-top: 20px;
        }

        .signup-button {
            background-color: #007BFF;
            color: #fff;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            transition: background-color 0.3s;
        }

        .signup-button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Reset password failed</h1>
        <p>Link is expired or invalid, <b>try again</b> to reset password.</p>
        <div class="button-container">
            <button class="signup-button" onclick="redirectToSignUp()">Reset Password</button>
        </div>
    </div>

    <script>
        function redirectToSignUp() {
            // You can specify the URL to your sign-up page here
            window.location.href = "/forgot-password";
        }
    </script>
</body>
</html>
